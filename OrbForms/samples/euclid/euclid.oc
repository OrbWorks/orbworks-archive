// application code

handler app.onstart() {
    // load the main form
    mainForm.load();
}

/* Greatest common divisor of the numerator and the denominator */
int gcd(int u, int v)
{
    int t=0;
    while (u>0)
    {
        if (u<v) { t=u; u=v; v=t;}
        u=u-v;
    }
    return v;
}


