// application code

// include for hookcontrast
#include "SampleAddIn.oc"

handler app.onstart() {
    hookcontrast(true);
    // load the main form
    mainForm.load();
}

handler app.onstop() {
    hookcontrast(false);
}
